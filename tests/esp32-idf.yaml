esphome:
  name: test-esp32-idf

esp32:
  board: esp32-c3-devkitm-1
  variant: esp32c3
  framework:
    type: esp-idf

logger:

web_server:
  version: 3
  auth:
    username: admin
    password: test-password
    type: digest

<<: !include common.yaml
